# Only needed if you switch to the optional LLM-backed provider
# (content_qa.llm_provider). The default deterministic provider needs no key
# at all. Copy this file to .env and never commit the real one.

OPENAI_API_KEY=your-openai-api-key-here
OPENAI_CHAT_MODEL=gpt-4o-mini
